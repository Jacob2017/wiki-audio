// Package feed renders the RSS XML (with iTunes podcast namespace)
// from the manifest using encoding/xml + struct tags (§5.7, §8.6).
// No third-party RSS library — the surface is ~60 lines.
package feed
